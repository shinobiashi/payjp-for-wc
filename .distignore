# Development & build tooling
.claude/
.github/
.nvmrc
.wp-env.json
node_modules/
vendor/
scripts/

# Test suites
tests/
test-results/
playwright.config.js
phpunit.xml

# PHP quality tools config
phpcs.xml
phpstan.neon
phpstan-bootstrap.php
phpstan-wcs-stubs.php

# JS build config
webpack.config.js
eslint.config.js
package.json
package-lock.json

# Composer manifests
composer.json
composer.lock

# Project documentation (dev-only)
docs/
CLAUDE.md
CONTRACT.md
DEVELOPMENT_PLAN.md
ESTIMATE.md
README.md
README-JA.md
TEST_CHECKLIST.md

# Git
.git/
.gitignore
.gitattributes
.gitkeep
.distignore

# WP.org directory assets (banners/icons go to SVN /assets/, not plugin ZIP)
.wordpress-org/

# Compiled PHP translations (.po/.mo) are delivered as language packs from
# translate.wordpress.org. They stay in the repository for the Japanized for
# WooCommerce bundle. The handle-named JS translation file
# (payjp-for-wc-ja-payjp-blocks-checkout.json) is KEPT because
# wp_set_script_translations() resolves it by script handle at runtime; only the
# intermediate per-source-file JSON files and the webpack map are excluded.
# `wp i18n make-json` names intermediate files payjp-for-wc-<locale>-<32-char
# md5-hash>.json (see scripts/merge-block-translations.js); matching by the
# fixed 32-character hash length (rather than hardcoding today's hash values)
# keeps this exclusion correct even after future source-file/build-path changes.
languages/*.po
languages/*.mo
languages/payjp-for-wc-webpack-map.json
languages/payjp-for-wc-*-????????????????????????????????.json

# Generated deliverables (not part of the plugin)
dist/

# Editor / tool caches
.vscode/
.phpunit.cache/

# Misc
messages.mo
*.DS_Store
.DS_Store
